import { mount } from "@vue/test-utils"
import WalletBinding from "@/common/components/WalletBinding"

describe("Common WalletBinding", () => {
  it("Should render", () => {
    mount(WalletBinding)
  })

})
